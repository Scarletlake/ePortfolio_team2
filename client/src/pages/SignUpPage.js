import React, { Fragment } from 'react';
import {userSignUp} from "../api/userAPI";

const NotFound = () => {
  return (
    <Fragment>
      <h1>
      SignUp
      </h1>
    </Fragment>
  );
};

export default NotFound;